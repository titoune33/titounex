// Pages RH non pertinentes pour TitouneOS — conservées mais non bloquantes pour le build
// Le dashboard unifié (dashboard), explorateur (explorer), workflows (workflows),
// IA (ai) et connecteurs (connectors) sont les pages principales de TitouneOS.

export {};
